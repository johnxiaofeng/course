#ifndef TYPES_H_
#define TYPES_H_

#include "Eigen/Dense"
#include <vector>

using Vector = Eigen::VectorXd;
using Matrix = Eigen::MatrixXd;

using Vectors = std::vector<Vector>;
using Matries = std::vector<Matrix>;

#endif //TYPES_H_
